# Piso comercial e preço-alvo

Piso final = `max` das regras configuradas:

- margem absoluta / L
- margem % sobre venda: `cost / (1 - pct)`
- markup: `cost × (1 + markup)`

Alvo usa regras equivalentes. **Alvo nunca fica abaixo do piso.**
