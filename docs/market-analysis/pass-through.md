# Índice de repasse

`pass_through_ratio = Δalvo / Δreferência`  
`pass_through_elasticity = %alvo / %referência`

Bloqueio: `|Δreferência| < minimum_reference_change` → `PASS_THROUGH_UNAVAILABLE`.

Separar altas e quedas. Assimetria = média altas − média quedas.
