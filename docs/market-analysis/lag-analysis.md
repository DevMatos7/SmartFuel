# Defasagem (lag)

Intervalo `[minimum_lag, maximum_lag]` configurável.

Lag positivo: alvo em `t` comparado à referência em `t - lag`.

O lag selecionado é a **defasagem de maior associação observada**, não prova causal.
