# Alinhamento temporal

Políticas: `EXACT_DATE`, `AGGREGATE_INTERNAL_TO_WEEKLY`, `CARRY_FORWARD_EXTERNAL`.

Carry-forward exige `carry_forward=true` e respeita `maximum_carry_forward_age`.

Não criar observações semanais artificiais no banco.
