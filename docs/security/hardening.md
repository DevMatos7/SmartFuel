# Hardening

- Deny by default + escopo org/posto
- Rate limit / refresh rotation
- Headers de segurança em produção
- Segredos via `secret_ref`
- Produção bloqueada com fonte UNSAFE (`sa`)
- Correlation ID em todas as respostas
