# Observabilidade

Logs estruturados, correlation ID (`X-Correlation-ID`), métricas técnicas e health checks.

- Live: processo vivo
- Ready: PostgreSQL obrigatório; Redis/MinIO degradam
- XPERT: dependência degradada/UNSAFE — **não** derruba a API

Ver também: `logging.md`, `metrics.md`, `health-checks.md`.
