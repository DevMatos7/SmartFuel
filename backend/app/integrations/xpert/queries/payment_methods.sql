-- Sprint 6 — contrato pendente de validação com DBA/XPERT.
-- Não inventar tabelas: substituir por SQL real antes de habilitar o dataset.
SELECT
    CAST(NULL AS VARCHAR(50)) AS source_payment_method_id,
    CAST(NULL AS VARCHAR(50)) AS source_payment_method_code,
    CAST(NULL AS VARCHAR(255)) AS source_payment_method_name,
    CAST(NULL AS BIT) AS source_active,
    CAST(NULL AS DATETIME2) AS source_updated_at
WHERE 1 = 0;
